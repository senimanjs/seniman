# Seniman with a Node entrypoint and TypeScript

This example keeps ownership of the Node HTTP server, serves `/api/health`, and passes remaining HTTP and WebSocket traffic to Seniman.

```bash
npm install
npm run typecheck
npm run dev
```
